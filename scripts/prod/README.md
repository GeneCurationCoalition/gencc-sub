# Production automation helpers (local-only)

These scripts are thin wrappers intended to support the Terraform/Ansible deployment workflow.

- `build-images.sh` builds local images for `gencc-sub` and `gencc-search` using Podman.

