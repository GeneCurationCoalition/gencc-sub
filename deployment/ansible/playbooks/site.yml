---
- name: Configure GenCC production VM (Podman + MySQL)
  hosts: gcp_vms
  become: true

  roles:
    - base
    - ops_scripts
    - mysql
    - quadlet
    - db_bootstrap
    - nginx_tls
    - timers
